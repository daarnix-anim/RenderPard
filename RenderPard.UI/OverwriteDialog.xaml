<Window x:Class="RenderPard.UI.OverwriteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Файл существует" Height="180" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Topmost="True">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Да" Width="70" Margin="5" Click="BtnYes_Click"/>
            <Button Content="Да для всех" Width="90" Margin="5" Click="BtnYesToAll_Click"/>
            <Button Content="Нет" Width="70" Margin="5" Click="BtnNo_Click"/>
            <Button Content="Нет для всех" Width="90" Margin="5" Click="BtnNoToAll_Click"/>
        </StackPanel>
    </Grid>
</Window>
