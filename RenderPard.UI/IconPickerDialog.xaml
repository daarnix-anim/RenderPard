<Window x:Class="RenderPard.UI.IconPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор иконки" Height="300" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Topmost="True" Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ListBox x:Name="IconsList" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5" Width="50" Height="60" ToolTip="{Binding Name}">
                            <Image Source="{Binding Image}" Width="32" Height="32" Stretch="Uniform" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Name}" FontSize="10" TextAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,4,0,0" Foreground="{StaticResource AppTextBrush}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Из файла..." Width="90" Margin="0,0,10,0" Height="28" Click="BtnBrowse_Click"/>
            <Button Content="Отмена" Width="80" Margin="0,0,10,0" Click="BtnCancel_Click" Height="28"/>
            <Button Content="ОК" Width="80" Click="BtnOK_Click" Height="28" Background="{StaticResource AppButtonBackgroundBrush}" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
